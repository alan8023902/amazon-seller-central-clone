# Sales Dashboard 1:1 像素级还原计划

## 目标
重写 Sales Dashboard 页面的内容区，使其与参考截图完全一致，包括布局、样式、组件结构和交互。

## 实现步骤

### 1. 移除顶部 Banner
- 删除或隐藏页面顶部的两个蓝色提示条（Update for Sales / Performance Alerts）
- 确保只影响当前页面，不影响其他页面

### 2. 重写页面标题行
- 左侧：大标题 "Sales Dashboard" + 右侧 "Learn more" 链接
- 右侧："Refresh" 按钮（白底描边 teal，文字 teal）和 "Download" 按钮（实心 teal，文字白）
- 使用 Tailwind CSS 实现精确的尺寸、间距和样式

### 3. 重写 Business Performance Insights 卡片
- 白底卡片，1px 细边框，小圆角
- 左侧标题："Business Performance Insights"
- 文案："You are all caught up! No new insights or actions since your last visit."
- 右侧轻量反馈链接："Help improve this experience" + 小图标

### 4. 重写筛选条 Filters
- 浅灰背景（#f2f3f3），1px 边框
- 三个字段块：
  - Date：下拉框（默认 "Custom"）+ 两个日期输入框（带日历 icon）
  - Sales breakdown：下拉框（默认 "Marketplace total"）
  - Fulfillment channel：下拉框（默认 "Both (Amazon and seller)"）
- 右侧 Apply 按钮（实心 teal）

### 5. 重写 Sales Snapshot
- 白底卡片 + 1px 边框
- 顶部标题行：左侧 "Sales Snapshot"，右侧 "taken at <timestamp> PST/UTC"
- 5 个等宽指标：Total order items、Units ordered、Ordered product sales、Avg. units/order item、Avg. sales/order item
- 每个指标：上方小 label（灰色），下方大数字（带千分位，金额带 $ 和两位小数）

### 6. 重写 Compare Sales
- 浅蓝色背景块
- 标题行：左侧 "Compare Sales"，右侧 Graph/Table toggle
- 两张并排折线图（各占 50%）：
  - 左图：Units ordered（y 轴 label）
  - 右图：Ordered product sales（y 轴 label）
  - 每张图 2 条线（teal + orange），折线密集
- 使用 Recharts 实现，支持 ResponsiveContainer

### 7. 添加 Mock 数据生成函数
- 输入：startDate/endDate + totalUnits + totalSales
- 生成 daily 数据数组，包含两条对比线（当前 vs 去年同期）
- 使用 seeded random 确保数据稳定

## 文件修改
- 只修改 `d:/amazon-seller-central-clone/features/BusinessReports.tsx` 文件
- 使用 Tailwind CSS 实现样式，尽量不修改全局 CSS
- 提供完整可覆盖的代码

## 预期效果
Sales Dashboard 页面内容区与参考截图 1:1 像素级一致，包括布局、样式、组件结构和交互。