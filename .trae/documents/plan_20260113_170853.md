## Implementation Plan

1. **Header height & alignment**:
   - Set header container height to `h-[41px]`
   - Use `flex items-center` for vertical centering
   - Remove extra vertical padding

2. **Left area modifications**:
   - Hamburger button: w-[32px] h-[41px], icon size 16px
   - Add 1px vertical divider right after hamburger, height ~24px
   - Replace ConsoleLogo with brand text: "amazon seller central" (lowercase)
   - Combine store + marketplace selector into one group: "TechNestGo | United States"
   - Group height ~24px, subtle border, small radius
   - Gap between brand and group ~12px

3. **Center search modifications**:
   - Separate input and button (not icon inside input)
   - Input: h-[28px], placeholder "Search", px-3
   - Button: w-[36px] h-[28px], magnifier icon centered
   - Visually connected, aligned center

4. **Right tools area modifications** (order from left to right):
   - Small toggle switch
   - Text label: "New Seller Central" (remove "+" icon)
   - Star icon button
   - Mail icon button
   - Gear icon button
   - Language selector: "EN" with caret
   - Text: "Help"
   - Icon buttons: w-[28px] h-[28px], icons 16px
   - Tight spacing: gap 10-12px

5. **Keep existing colors unchanged**

**File to modify**: `layouts/MainLayout.tsx`

**Key components to update**:
- Header container
- Left area (hamburger, divider, brand, store/marketplace selector)
- Center search
- Right tools area

**Note**: Maintain all existing functionality, just update the layout and styling to match the real Amazon Seller Central header.